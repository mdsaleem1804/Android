<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nellaibill.petcollege">

    <uses-permission android:name = "android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name = "android.permission.INTERNET" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/pet_bell"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/pet_bell"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomePage"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StudentHomePage" />
        <activity android:name=".Webform" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".FeesActivity" />
        <activity android:name=".ListDepartment" />
        <activity android:name=".ListYear" />
        <activity android:name=".TimeTable" />
        <activity
            android:name=".AdminHomePage"
            android:label="@string/title_activity_admin_home_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".StudentLogin"
            android:label="@string/title_activity_home_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AdminLogin" />
        <activity android:name=".FacultyDepartment" />
        <activity android:name=".NewsFeed" />
        <activity
            android:name=".CommentForNews"
            android:label="@string/title_activity_comment_for_news"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".LibraryLogin"
            ></activity>
        <activity android:name=".LibraryHomePage"></activity>
        <activity android:name=".AddBooks"></activity>
        <activity android:name=".IssueBooks"></activity>
        <activity android:name=".StaffLogin"></activity>
        <activity android:name=".StaffHomePage"></activity>
        <activity android:name=".StaffRegistration"></activity>
        <activity android:name=".ListStaff"></activity>
        <activity android:name=".StaffLocation"></activity>
        <activity android:name=".ViewStaffLocation"></activity>
        <activity android:name=".AddHospitalData"></activity>
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>