package com.hellotamila.ah_and_009_hospial;

import android.content.Intent;
import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;
import android.view.View;
import android.widget.ArrayAdapter;
import android.widget.Spinner;
import android.widget.Toast;

import java.util.ArrayList;
import java.util.List;

public class Medical extends AppCompatActivity {
    Spinner sItems;
    SimpleGetterAndSetter obj;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.medical);
        obj = new SimpleGetterAndSetter();
        ArrayList<String> spinnerArray =  new ArrayList<String>();
        spinnerArray.add("Apollo Pharmacy");
        spinnerArray.add("MedPlus");
        spinnerArray.add("Vasan Medicals");
        spinnerArray.add("Krish Medicals");

        ArrayAdapter<String> adapter = new ArrayAdapter<String>(
                this, android.R.layout.simple_spinner_item, spinnerArray);

        adapter.setDropDownViewResource(android.R.layout.simple_spinner_dropdown_item);
        sItems = (Spinner) findViewById(R.id.spinner);
        sItems.setAdapter(adapter);


    }

    public void order(View v) {
       obj.setMedLatLong("8.711752","77.750817");
        Toast.makeText(getApplicationContext(),sItems.getSelectedItem().toString(),Toast.LENGTH_LONG).show();
       Intent xIntent = new Intent(getApplicationContext(), Webform.class);
        startActivity(xIntent);
    }
}















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































